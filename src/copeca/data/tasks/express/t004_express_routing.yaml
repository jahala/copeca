name: t004_express_routing
description: "Map how app.use() builds Express's layer stack and routes requests through multiple middleware; tests framework internals tracing across Router and Layer."
source: "SWE-QA (Apache-2.0)"
repo: express
type: comprehension
category: trace
language: javascript
difficulty: medium
version: 1
prompt: |
  Find the `Router` class in the Express codebase and trace how `app.use()`
  registers middleware. Explain how routes are stored internally, how the
  layer stack is constructed, and what happens when a request matches multiple
  middleware functions.
ground_truth:
  required_strings:
    - Router
    - use
    - Layer
    - handle
    - middleware
  all_of:
    - Router
    - Layer
    - handle
  forbidden_strings:
    - "I cannot"
    - "unable to"
