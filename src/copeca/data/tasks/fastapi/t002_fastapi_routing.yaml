name: t002_fastapi_routing
description: "Find the APIRouter class in FastAPI and describe how route handlers are registered."
source: "SWE-QA (Apache-2.0)"
repo: fastapi
type: comprehension
language: python
difficulty: medium
version: 1
prompt: |
  Find the `APIRouter` class in the FastAPI codebase and describe how route
  handlers are registered. Explain the relationship between `APIRouter`,
  `Route`, and the `add_api_route` method. What decorators does APIRouter
  provide for HTTP methods?
ground_truth:
  required_strings:
    - APIRouter
    - add_api_route
    - Route
    - get
    - post
  all_of:
    - APIRouter
    - add_api_route
    - Route
  forbidden_strings:
    - "I cannot"
    - "unable to"
