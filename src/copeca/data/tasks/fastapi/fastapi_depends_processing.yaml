name: fastapi_depends_processing
description: "Trace FastAPI's Depends from its definition in param_functions.py through the dependency-tree resolver that executes each Depends() call at request time; tests multi-file tracing of the full dependency injection path."
source: "tilth-benchmark (MIT)"
repo: fastapi
commit: 6fa573ce0bc16fe445f93db413d20146dd9ff35d
type: comprehension
category: trace
language: python
difficulty: medium
version: 1
prompt: |
  Find the Depends function in fastapi/param_functions.py and show
  its implementation. Then find how FastAPI processes dependencies
  — look for the function that walks the dependency tree and resolves
  each Depends() call at request time.
ground_truth:
  required_strings:
    - def Depends
    - solve_dependencies
    - Dependant
    - get_dependant
