name: fastapi_depends_internals
description: "Locate the Depends function in fastapi/param_functions.py, reveal its return type, and identify where the underlying implementation lives; tests distinguishing a public API wrapper from its underlying class."
source: "tilth-benchmark (MIT)"
repo: fastapi
commit: 6fa573ce0bc16fe445f93db413d20146dd9ff35d
type: comprehension
category: locate
language: python
difficulty: medium
version: 1
prompt: |
  In fastapi/param_functions.py, find the Depends function. Show its complete
  signature including all parameters. Then explain: is Depends a class or a function?
  What does it actually return, and where is the underlying implementation?
ground_truth:
  required_strings:
    - def Depends
    - use_cache
    - params.Depends
