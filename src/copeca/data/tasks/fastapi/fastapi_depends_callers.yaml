name: fastapi_depends_callers
description: "Map FastAPI's Depends function — full signature, return type, home file, and direct callers across the codebase; tests combining definition-reading with cross-file caller enumeration."
source: "tilth-benchmark (MIT)"
repo: fastapi
commit: 6fa573ce0bc16fe445f93db413d20146dd9ff35d
type: comprehension
category: trace
language: python
difficulty: medium
version: 1
prompt: |
  Give me a complete picture of FastAPI's `Depends` function: its
  full signature, what it actually returns, the file it lives in,
  and which functions in the codebase call it directly.
ground_truth:
  required_strings:
    - def Depends
    - use_cache
    - params.Depends
